spring.application.name=Mock_Interview_Project

# Database - uses environment variables in production, falls back to local for dev
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/mockdb}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:8767338142@vdVD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.thymeleaf.cache=false
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.type.descriptor.sql=trace
